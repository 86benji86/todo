import { Container } from "react-bootstrap";
import 'bootstrap/dist/css/bootstrap.min.css';

const Header = () => {
    return (
        <Container fluid>
            <h1 className="text-center">Header - ToDo List</h1>
        </Container>
      );
}
 
export default Header;